import IRider from "../interfaces/IRider";

class DriverService {
    public create (data: IRider): void
    {
        return;
    }

    public find (query?: string, fields?: string[]): void
    {
        return;
    }
}

export default new DriverService();